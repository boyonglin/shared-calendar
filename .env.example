# ===========================================
# Shared Calendar - Environment Variables
# ===========================================
# This file documents ALL environment variables used in the project.
#
# FOR LOCAL DEVELOPMENT:
#   - Copy server variables to server/.env
#   - Copy client variables to client/.env
#
# FOR VERCEL DEPLOYMENT:
#   - Set all variables in Vercel Dashboard > Settings > Environment Variables
#
# ===========================================

# ===========================================
# SERVER VARIABLES (server/.env)
# ===========================================

# Server Configuration
NODE_ENV=development
PORT=3001
CLIENT_URL=http://localhost:5173

# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3001/api/auth/google/callback

# OneCal Configuration (Optional - for Outlook Calendar integration)
# Get these from: https://onecal.io/
# ONECAL_APP_ID=
# ONECAL_API_KEY=

# Security
# Generate with: openssl rand -base64 48
JWT_SECRET=your-jwt-secret-at-least-32-characters-long-for-security

# Encryption Key for iCloud passwords
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-64-character-hex-encryption-key-here-32bytes

# ===========================================
# CLIENT VARIABLES (client/.env)
# ===========================================

# API Base URL (for production, use your Vercel URL)
VITE_API_BASE_URL=http://localhost:3001

# ===========================================
# Vercel/Turso Deployment (Production Only)
# ===========================================
TURSO_DATABASE_URL=libsql://your-db-turso.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token